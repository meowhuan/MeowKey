<Page
    x:Class="MeowKey.Manager.Pages.SecurityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MeowKey.Manager.Models">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="12" Padding="0,0,0,12">
            <Border Style="{StaticResource SectionCardStyle}">
                <Grid ColumnSpacing="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="8">
                        <TextBlock x:Name="PageTitleText" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock x:Name="PageDescriptionText"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}"
                                   TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                Spacing="8"
                                VerticalAlignment="Top">
                        <Button x:Name="RefreshSecurityButton"
                                Click="OnRefreshSecurity" />
                        <Button x:Name="KeepDebugLimitedButton"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="OnKeepDebugLimited" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource SectionCardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock x:Name="UserPresenceTitleText" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock x:Name="UserPresenceDescriptionText"
                               Foreground="{ThemeResource AppMutedForegroundBrush}"
                               TextWrapping="WrapWholeWords" />

                    <ItemsControl ItemsSource="{x:Bind Snapshot.UserPresenceSections}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:UserPresenceSection">
                                <Border Style="{StaticResource SurfaceCardStyle}" Margin="0,0,0,8">
                                    <StackPanel Spacing="10">
                                        <TextBlock Text="{x:Bind Title}"
                                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                                        <TextBlock Text="{x:Bind Status}" FontSize="18" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Detail}"
                                                   Foreground="{ThemeResource AppMutedForegroundBrush}"
                                                   TextWrapping="WrapWholeWords" />

                                        <ItemsControl ItemsSource="{x:Bind Rows}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="models:InfoItem">
                                                    <Border BorderBrush="{ThemeResource AppCardBorderBrush}"
                                                            BorderThickness="0,0,0,1"
                                                            Padding="0,0,0,8"
                                                            Margin="0,0,0,8">
                                                        <StackPanel Spacing="4">
                                                            <TextBlock Text="{x:Bind Label}"
                                                                       Foreground="{ThemeResource AppMutedForegroundBrush}" />
                                                            <TextBlock Text="{x:Bind Value}"
                                                                       FontWeight="SemiBold"
                                                                       TextWrapping="WrapWholeWords" />
                                                            <TextBlock Text="{x:Bind Detail}"
                                                                       Foreground="{ThemeResource AppMutedForegroundBrush}"
                                                                       TextWrapping="WrapWholeWords" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SectionCardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock x:Name="ManageUpTitleText" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock x:Name="ManageUpDescriptionText"
                               Foreground="{ThemeResource AppMutedForegroundBrush}"
                               TextWrapping="WrapWholeWords" />

                    <Grid ColumnSpacing="12" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="UpSourceLabelText"
                                   Grid.Row="0"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <ComboBox x:Name="UpSourceCombo"
                                  Grid.Row="0"
                                  Grid.Column="1">
                            <ComboBoxItem x:Name="UpSourceNoneItem" />
                            <ComboBoxItem x:Name="UpSourceBootselItem" />
                            <ComboBoxItem x:Name="UpSourceGpioItem" />
                        </ComboBox>

                        <TextBlock x:Name="UpGpioPinLabelText"
                                   Grid.Row="1"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <TextBox x:Name="UpGpioPinTextBox"
                                 Grid.Row="1"
                                 Grid.Column="1" />

                        <TextBlock x:Name="UpTapCountLabelText"
                                   Grid.Row="2"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <TextBox x:Name="UpTapCountTextBox"
                                 Grid.Row="2"
                                 Grid.Column="1" />

                        <TextBlock x:Name="UpGestureWindowLabelText"
                                   Grid.Row="3"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <TextBox x:Name="UpGestureWindowTextBox"
                                 Grid.Row="3"
                                 Grid.Column="1" />

                        <TextBlock x:Name="UpRequestTimeoutLabelText"
                                   Grid.Row="4"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <TextBox x:Name="UpRequestTimeoutTextBox"
                                 Grid.Row="4"
                                 Grid.Column="1" />

                        <TextBlock x:Name="UpActiveLowLabelText"
                                   Grid.Row="5"
                                   Grid.Column="0"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource AppMutedForegroundBrush}" />
                        <CheckBox x:Name="UpActiveLowCheckBox"
                                  Grid.Row="5"
                                  Grid.Column="1" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="ApplyPersistedUpButton"
                                Click="OnApplyPersistedUserPresence" />
                        <Button x:Name="ApplySessionUpButton"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="OnApplySessionUserPresence" />
                        <Button x:Name="ClearSessionOverrideButton"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="OnClearSessionOverride" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <ItemsControl ItemsSource="{x:Bind Snapshot.SecurityPolicies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:PolicyItem">
                        <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,12">
                            <StackPanel Spacing="6">
                                <TextBlock Text="{x:Bind Title}"
                                           Foreground="{ThemeResource AppMutedForegroundBrush}" />
                                <TextBlock Text="{x:Bind Value}" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind Detail}"
                                           TextWrapping="WrapWholeWords" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Style="{StaticResource SectionCardStyle}">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="RecommendationTitleText" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock x:Name="RecommendationDescriptionText"
                               Foreground="{ThemeResource AppMutedForegroundBrush}"
                               TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
